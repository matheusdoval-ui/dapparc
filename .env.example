# ===========================================
# Variáveis para desenvolvimento e Vercel
# Copie para .env.local e preencha os valores.
# No Vercel: Settings → Environment Variables
# ===========================================

# ARC Testnet RPC (usado em API e server)
ARC_RPC_URL=https://rpc.testnet.arc.network
NEXT_PUBLIC_ARC_RPC=https://rpc.testnet.arc.network

<<<<<<< HEAD
# RPC para cliente — OBRIGATÓRIO para leaderboard reads (JsonRpcProvider)
# Copie para .env.local: NEXT_PUBLIC_ARC_RPC=https://rpc.testnet.arc.network
NEXT_PUBLIC_ARC_RPC=https://rpc.testnet.arc.network

# Contrato Leaderboard / Registry (deployado na Arc Testnet)
# Use o mesmo valor em ambos para cliente e API funcionarem.
NEXT_PUBLIC_REGISTRY_CONTRACT_ADDRESS=0x7461Fc5EAf38693e98B06B042e37b4adF1453AbD
REGISTRY_CONTRACT_ADDRESS=0x7461Fc5EAf38693e98B06B042e37b4adF1453AbD
=======
# Backend relayer: chave privada do owner do contrato ArcLeaderboard (POST /api/leaderboard)
# Obrigatório para salvar usuários on-chain. Adicione no Vercel: Settings → Environment Variables
LEADERBOARD_OWNER_KEY=0x...

# Tokens Arc Testnet (cliente: saldos USDC/EUR no card)
NEXT_PUBLIC_USDC_ADDRESS=0x3910B7cbb3341f1F4bF4cEB66e4A2C8f204FE2b8
NEXT_PUBLIC_EUR_ADDRESS=0x89B50855Aa3bE2F677cD6303Cec089B5F319D72a

# Faucet (backend): env only, no hardcoded keys
FAUCET_PRIVATE_KEY=0x...
USDC_TOKEN=0x...
EUR_TOKEN=0x...
# Amount to send per request (default 1). Use 10 for 10 tokens.
# FAUCET_AMOUNT=1

# Contrato Leaderboard (SAFE submitScore; deploy via deploy-leaderboard-hardhat.js)
# Use o mesmo valor em ambos para cliente e API funcionarem. App deve usar SOMENTE este endereço.
NEXT_PUBLIC_REGISTRY_CONTRACT_ADDRESS=0x...
REGISTRY_CONTRACT_ADDRESS=0x...

# Leaderboard do jogo (OBRIGATÓRIO após deploy do novo contrato que emite ScoreSubmitted):
# Contrato: submitScore(uint256) + bestScore(address) + ScoreSubmitted event
NEXT_PUBLIC_LEADERBOARD_CONTRACT_ADDRESS=0xc8ae199e116532Fd58AF86f9070f910C8D9b04e5
>>>>>>> 3813cb1 (deploy)

# Account Abstraction (opcional; padrões já funcionam na Arc)
# NEXT_PUBLIC_BUNDLER_URL=
# NEXT_PUBLIC_PAYMASTER_URL=
# NEXT_PUBLIC_PAYMASTER_ADDRESS=
# NEXT_PUBLIC_ENTRY_POINT_ADDRESS=0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789

# Game leaderboard (persistent, Supabase)
# Run supabase-leaderboard-schema.sql in Supabase SQL Editor, then set:
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=eyJ...
# Indexer (cron): use SERVICE_ROLE_KEY for writes if RLS is on
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
# RPC for indexer (default: Arc Testnet)
RPC_URL=https://rpc.testnet.arc.network
# Force full reindex from block 0 (set to 1 once, then remove): FORCE_REINDEX=1

# KV / Neon (se usar leaderboard em DB)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# DATABASE_URL=
